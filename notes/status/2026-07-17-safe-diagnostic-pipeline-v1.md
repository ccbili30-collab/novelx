# Safe Diagnostic Pipeline v1 status

Date: 2026-07-17
Branch: `codex/hackathon-10day`
Baseline HEAD: `60b097ed9a94be938837ab55008292b8b1372d99`
Commit state: unstaged and uncommitted; this batch remains mixed with the pre-existing interactive-Growth WIP and must be reviewed before any semantic staging.

## Implemented boundary

- Added strict `SafeDiagnosticEnvelopeV1` and module-owned immutable catalogs. Arbitrary messages, details, stacks, prompts, arguments, paths, URLs and credentials are not contract fields.
- Added additive SQLite schema v27 and append-only `SafeDiagnosticRepository` with exact replay, sequence, parent-context and persistence failure checks. Older audit rows are not rewritten and missing historical diagnostics are not fabricated.
- Connected trusted Provider, Worker schema/phase correction, Tool Bridge/Main, Gateway/Domain, image, Growth terminal and recovery boundaries.
- Growth interruption and unknown Change Set/Closure outcomes persist `outcome_unknown` diagnostics and require reconciliation; they are never automatically retried.
- Main projects only persisted allowlisted diagnostic fields into the Growth snapshot. Renderer groups them under the owning Cycle and displays owner, stable code and side-effect state without raw failure material.
- Diagnostic persistence remains fail-closed. The diagnostic repository never recursively diagnoses its own write failure.

## Verification

- Full `npm test` after the Revision alias correction: 135 files, 858 tests passed, 0 failed, 0 skipped.
  - unit: 131 files / 828 tests.
  - integration: 3 files / 22 tests.
  - e2e-support: 1 file / 8 tests.
- `npm run typecheck`: passed.
- `npm run verify:prompt-publication`: passed; active prompts remain 3.
- `npm run build`: passed. Existing `SemanticGraphView` mixed static/dynamic import warning remains non-blocking.
- `npx --no-install playwright test tests/e2e/growth-presentation-ui.spec.ts --workers=1`: 3/3 passed without a Provider; includes strict safe diagnostic projection and fail-closed UI behavior.
- `git diff --check`: passed.
- Worktree Electron residue: 0.

### 2026-07-17 diagnostic precision extension

- Image `/v1/responses` non-2xx failures now map by status into stable allowlisted classes. The class is persisted on the failed Image Job and projected into the Main-only safe diagnostic, while the Worker and ordinary tool audit continue to receive only `IMAGE_GENERATION_FAILED`.
- Revision reference failures now distinguish invalid document owner, assertion scope, source document, relation endpoint and resource parent. Fixed correction messages contain no invalid ID or model content.
- Growth public diagnostics now retain the already-persisted `attempt/maxAttempts` pair, and the Renderer can display `第 x/y 次` without inferring it from text.
- Deterministic extension verification: 10 focused files / 131 tests passed; `npm run typecheck` passed after correcting the failed-image component test file extension from `.ts` to `.tsx`.
- No Provider, Electron Live, full suite, package, stage, commit or push was run for this extension. Historical evidence cannot be retroactively upgraded from `IMAGE_GENERATION_FAILED` to a specific HTTP class because the old run did not retain that fact.

### 2026-07-17 precise replay and Revision reference correction

- One authorized replay used `openai-compatible / gpt-5.4` and `openai-compatible-image / gpt-image-2`. Cycle 1 committed and its world-map Job succeeded with one Asset. The earlier image failure did not reproduce.
- Cycle 2 retrieved nine pinned links and then failed before the Change Set executor. The new diagnostics recorded, in order: `GROWTH_REVISION_FRAGMENT_IMPACT_MISMATCH` attempt 1/3, `GROWTH_REVISION_FRAGMENT_OWNER_REF_INVALID` attempt 2/3, and terminal `GROWTH_REVISION_FRAGMENT_RELATION_ENDPOINT_REF_INVALID` attempt 3/3. Every failure had `sideEffectState=none`; Cycle 2 created no Change Set or output checkpoint.
- Evidence: `growth-guidance-live-2026-07-16T18-48-26-415Z.json`; SHA-256 `C13A32BC4C3B1B3DDF40D8AEA4D819CE9423A5F4F7B81DE1ADFEBA87C75726DF`; UTF-8 parse and leak scan passed.
- Code review found a concrete Harness ambiguity: model-visible retrieval exposed several real identity shapes while the compiler accepted only evidence IDs or local IDs. The Revision model contract now exposes deterministic disjoint aliases, rejects raw persisted identities as existing targets, and derives addition kinds from the actual mutation arrays. Rejected Inquiry calls with no side effects are now diagnostic `observed/model_correction`, not false terminal facts.
- The correction has deterministic evidence only. No second Provider replay has run after the alias change, so Revision Live remains unproven.
- The frozen correction passed the full 135-file suite: 828 unit, 22 integration and 8 E2E-support tests, with zero failures and zero skips. The full-suite run also caught and closed stale diagnostic-catalog expectations and a theme-token regression in the failed-image placeholder.

### 2026-07-17 post-correction Live replay

- One post-correction replay used `openai-compatible / gpt-5.4` and `openai-compatible-image / gpt-image-2`; no retry was run.
- Cycle 1 committed and its world-map Job succeeded. Cycle 2 recorded an 11-link Receipt and selected its Inquiry, then blocked before a Change Set existed.
- The previous Fragment correction sequence (`IMPACT_MISMATCH`, `OWNER_REF_INVALID`, `RELATION_ENDPOINT_REF_INVALID`) did not recur. The compiled proposal reached Main's Revision proposal authorization and was rejected as `GROWTH_BINDING_INVALID` at `main_gateway/tool_authorization`, with `sideEffectState=none` and no Change Set/output checkpoint.
- The current Main proposal policy collapses multiple independent authorization predicates into that one code. This evidence therefore proves the new boundary but does not identify the exact rejected predicate; assigning a more specific cause would be speculation.
- Evidence: `growth-guidance-live-2026-07-16T19-12-22-796Z.json`; SHA-256 `58AB4608467EC266CEEC25C60E7F4396639C65A2D1BEBC68FB160D5E1222C8C7`; UTF-8 parse and leak scan passed. Worktree Electron residue after the run was zero.

### 2026-07-17 Revision policy diagnostic candidate R2

- Main Revision proposal authorization now distinguishes ten module-local policy failures: item graph, impact authority/set conflict, existing/created identity, owner, assertion source, relation endpoint, forbidden mutation and actual mutation-set mismatch.
- The Worker-facing failure remains `GROWTH_BINDING_INVALID`; only the persisted allowlisted diagnostic retains the exact policy code. Proposal content, IDs and raw errors are not diagnostic fields.
- Compiler-to-Main characterization covers existing document edits, legal cross-domain relations, resource/document/assertion additions, relation removal and forged low-level proposals.
- Frozen deterministic acceptance: 6 focused files / 74 tests passed; full `npm test` 136 files / 872 tests passed (842 unit, 22 integration, 8 E2E-support), zero failed and zero skipped; typecheck, active Prompt publication, production build and diff check passed; Electron residue zero.
- R2 Live evidence: `growth-guidance-live-2026-07-16T19-47-14-090Z.json`, SHA-256 `0691AD028B3755BE190AF27428F4BDF9B7A883380F3F6115887CF7C54FB3588D`, `leakScan=passed`.
- Cycle 1 committed. Cycle 2 first recorded `GROWTH_REVISION_FRAGMENT_RELATION_ENDPOINT_REF_INVALID` as a correctable compile error, then recorded the same code as corrected on attempt 2.
- The corrected proposal then reached Domain Policy and was rejected with `GREENFIELD_CREATE_EXPLICIT_FREE_REQUIRED`, `sideEffectState=none`; no second Change Set was created.

### 2026-07-17 Revision same-Change-Set evidence candidate R3

- Root cause: Revision policy intentionally permits new documents and assertions in one atomic Change Set, but Gateway and Workspace policy treated `greenfield_document_output:*` evidence as Greenfield-only. A legal bounded Revision was therefore misclassified after its Main authority check had already passed.
- R3 adds a Main-only `revisionSameChangeSetEvidenceAuthorized` path. It is set only after `assertGrowthRevisionProposalAllowed`, is not model/Renderer/public-protocol input, and only permits an assertion to cite a dependent `document.put` output from the same proposal.
- Characterization: the Growth Run bridge now commits a pinned Revision that updates an existing world and atomically adds a location, its profile document and a sourced assertion. Ordinary Greenfield and workspace policy tests remain green.
- Freeze evidence: 7 focused files / 105 tests passed; full suite 136 files / 872 tests passed with zero skips; typecheck, active Prompt gate (3), production build and diff checks passed; Electron residue=0; staged=0.
- R3 final Live evidence: `growth-guidance-live-2026-07-16T20-01-50-985Z.json`, SHA-256 `69173911C171120B2628334E50A6326D55ADD60BEBFF4F8CD5F1AEA98B8987C2`, `leakScan=passed`.
- R3 fixed the targeted boundary in a real run: Cycle 1 and Cycle 2 both committed; two Change Sets and two checkpoint advances were persisted. The prior `GREENFIELD_CREATE_EXPLICIT_FREE_REQUIRED` rejection did not recur.
- Cycle 3 was persisted as `planned` but had no Run/Receipt/Change Set. The E2E then failed while polling `growth.get` with the public aggregate `Growth Run bridge failed` message.
- No Safe Diagnostic record was persisted for this new bridge boundary. Current evidence cannot distinguish Cycle 3 startup, route recovery or snapshot projection, so assigning a narrower cause would be speculation.
- The bounded loop is terminal after L3: no R4 and no unchanged Provider replay. Recovery requires a separately authorized diagnostic task for the planned-Cycle `growth.get` bridge boundary.

## Live and evidence boundary

- Exactly one real interactive-Growth replay ran after the deterministic diagnostic acceptance, using `openai-compatible / gpt-5.4`. It failed at the public aggregate assertion `GROWTH_COORDINATOR_TERMINAL_NOT_COMPLETED`; no retry was performed.
- Sanitized evidence: `growth-guidance-live-2026-07-16T17-52-03-491Z.json`; SHA-256 `CA6F6CE4CBBBAF0E409AC6F57F49AC20681EF17459614704FCD85B8FCAB2E272`; UTF-8 JSON parsing and the harness leak scan passed.
- The persisted diagnostics identify two independent boundaries:
  - Cycle 1 map generation: owner `provider`, boundary `asset_commit`, code `IMAGE_GENERATION_FAILED`, tool `generate_image`, side effect `request_sent`, terminal disposition, retryability `user_action`. The world Change Set committed, but the image Job failed and no Asset was created.
  - Cycle 2 revision: owner `growth_phase`, boundary `phase_compile`. Two `GROWTH_REVISION_FRAGMENT_REFERENCE_INVALID` diagnostics were model-correctable with no side effects; the third attempt ended as terminal `GROWTH_REVISION_FRAGMENT_INVALID`, followed by the Cycle rollup `GROWTH_CHANGE_SET_NOT_COMMITTED`. No Change Set or output checkpoint was created for Cycle 2.
- This replay proves the safe diagnostic pipeline can locate responsibility, boundary, stable code, side-effect state, disposition and retryability for these failures. It does not prove that every possible failure boundary is instrumented, and it is not a successful interactive-Growth Live acceptance.
- The five pre-existing Player failure evidence files remain unmodified and uncommitted.

## Incomplete and recovery entry

### 2026-07-17 Closure continuation and latest bounded Longform boundary

- Real evidence `growth-guidance-live-2026-07-17T13-12-11-923Z.json` (SHA-256 `2548DD9A338760D53CACDA3AD48B9ED521E6B698B3B6A74E9899EB7231B071A1`) proved world/story/OC, guidance, map and a complete 11,113-code-point Longform could reach a second Closure evaluation. That evaluation persisted `continue_growing`, but Main had no successor when Longform was already complete.
- A phase-local `growthClosureContinuationPlanner` now routes persisted unsatisfied `closure.world.*`, `closure.story.*` and `closure.oc.*` facets into one existing `revision` Cycle. An identical missing-facet fingerprint repeated across two evaluations records `GROWTH_CLOSURE_NO_PROGRESS` and stops instead of self-questioning forever. No public protocol, migration or domain rule changed.
- Frozen deterministic acceptance after this repair: full `npm test` 137 files / 899 tests passed (868 unit, 22 integration, 9 E2E-support), zero skips; typecheck, three Prompt publication gates, production build and diff check passed; Electron residue zero.
- The final authorized Live did not reach the repaired Closure branch. Evidence `growth-guidance-live-2026-07-17T13-44-50-092Z.json`, SHA-256 `00A87719D2985B550A8A3B3FD25C536F296EEFA4B6E0A6DF5336865448C42C7A`, parsed in PowerShell and Node with `leakScan=passed`.
- In that run C1-C8 committed/evaluated as expected and the world-map Job succeeded. Longform reached 7,391 code points in two sections. C9 accumulated bounded Writer continuations but the combined third section remained outside its deterministic length range for all three allowed corrections; no `propose_change_set` call or partial C9 Change Set occurred. C9 safely blocked as `GROWTH_CHANGE_SET_NOT_COMMITTED`.
- Product decision after the 13:44 boundary: 7–8 千字的波动可接受。Longform 的完成最低值现由 `GROWTH_LONGFORM_MIN_CODE_POINTS=7_000` 统一驱动 outline、progress、Closure、Coordinator 与 Live 验收；章节仍须符合各自 outline 范围、来源、顺序、无 filler 和 Checker 约束。旧持久化 facet ID `closure.oc.structure.personal_story_10000` 仅为兼容身份，不再是门槛权威。下一候选可运行一次新 Live，以证明 7,000 字符后进入 Closure continuation；不得把门槛调整描述成已通过的 Live 结果。
- The first real candidate after that decision produced `growth-guidance-live-2026-07-17T14-48-38-494Z.json` (SHA-256 `A55691E13C1931582B4684DCCFE413995C84D1E2C96BAC1A9951BA52A2AEC716`; PowerShell/Node JSON parse and `leakScan=passed`). Longform completed at 7,929 code points / 3 sections, proving the shared 7,000 threshold reached Closure. C1-C9 committed/evaluated and the world map succeeded; C10 evaluated `continue_growing` but recorded `GROWTH_CLOSURE_NO_PROGRESS` before any Closure-targeted revision had run.
- Root cause and repair: the initial continuation planner counted two identical Closure evaluations as no progress even when only unrelated Longform `expand` Cycles occurred between them. It now stalls only when a committed `revision` covering all affected focus kinds occurred between identical evaluations; unrelated Longform work permits the first actual Closure revision. A deterministic regression fixes the exact C5 → Longform expands → C10 pattern. This repair still requires a new frozen Live candidate before it can be called accepted.
- The next real candidate produced `growth-guidance-live-2026-07-17T15-07-03-169Z.json` (SHA-256 `67B6E76021AEB6C8944D43A300E1424FDB3BBE1C6A45A545E09422C0F9F10642`; JSON parse and `leakScan=passed`). It stopped earlier at C7: Longform outline committed, retrieval carried 20 Receipt links, but Writer returned `missing_source`; no section Change Set occurred. The Harness now performs one content-free recheck only when every Writer reason is `missing_source`: it states that the pinned values are present, explicitly permits a repeated blocked result when semantically insufficient, and forbids invented/substituted evidence. A second refusal remains terminal. This is a bounded evidence recheck, not authority to fabricate prose.
- The following candidate reached and committed the first Closure-targeted C11 revision, but `growth.get` then failed while synchronizing the Closure profile. Evidence: `growth-guidance-live-2026-07-17T16-04-32-440Z.json`, SHA-256 `E76DCB3A20CAC6F7147305552EAB2320AF76D66B697407F6E22710D1F0DECD31`; UTF-8 JSON parse and `leakScan=passed`. Persisted post-termination state shows C1-C4/C6-C9/C11 committed, C5/C10 evaluated, and formal counts 11 resources / 15 documents / 9 assertions / 15 relations.
- Root cause: `growthRevisionClosureSync` required the Closure profile's rule revision to be strictly older than every committed content `revision`. Closure continuation intentionally commits a new checkpoint under the same pinned rule revision, so the valid C11 transition was rejected after its Change Set committed. The synchronizer now accepts equal rule revisions only when the current Closure checkpoint exactly equals the Cycle input checkpoint; discontinuous checkpoints and rule-version rollback remain fail-closed. Three focused synchronization tests cover same-rule advance, newer-rule advance and both rejection paths.
- Frozen deterministic acceptance after that repair: full `npm test` 138 files / 905 tests passed (874 unit, 22 integration, 9 E2E-support), zero failures and zero skips; typecheck, active Prompt publication (3), production build and diff check passed; Electron residue zero. A new real Provider replay is still required before the repaired continuation can be called accepted.
- The next replay proved that same-rule Closure synchronization: C11 committed, C12 completed a third Closure evaluation, and the earlier synchronization exception did not recur. Evidence: `growth-guidance-live-2026-07-17T16-32-30-478Z.json`, SHA-256 `39B960275B17D66CE6CDA800104CFF6ABD35920647C35400A95E76EB77603AA0`; UTF-8 JSON parse and `leakScan=passed`. It then failed because the new checkpoint projected Longform as blocked and the Longform coordinator threw before Closure continuation could own the repair.
- Recovery routing now distinguishes initial bootstrap from later invalidation: only a missing initial personal-story binding starts the outline path; any later blocked Longform invariant returns `not_applicable` to the Longform coordinator so persisted Closure gaps can drive the bounded revision/no-progress logic. The safe E2E evidence shape now retains the allowlisted `GrowthLongformProgressBlockedReason` instead of only the aggregate `blocked` state. This candidate still requires full freeze and a new Live replay.
- Frozen acceptance for that routing candidate: full `npm test` 138 files / 906 tests passed (875 unit, 22 integration, 9 E2E-support), zero failures and zero skips; typecheck, active Prompt publication (3), production build and diff check passed; Electron residue zero.
- Its one Live replay did not exercise the later invalidation route because model variability stopped earlier at the first Longform section. Evidence: `growth-guidance-live-2026-07-17T16-50-37-639Z.json`, SHA-256 `A23B43DFAE0FF5924944B191971B9BACF27B4DBE68A36FF89A7DE25D9CC9751A`; UTF-8 JSON parse and `leakScan=passed`. C1-C6 committed/evaluated, the map succeeded, and the Longform outline committed. C7 had a 24-link Receipt, but Writer returned `missing_source` on both the initial call and the single content-free recheck. It produced no proposal or partial Change Set and blocked as `GROWTH_CHANGE_SET_NOT_COMMITTED`.
- This is now a product-authority boundary, not an unclassified program failure: evidence-grounded creative prose needs an explicit rule permitting new narrative events inside pinned Canon/outline constraints. An unchanged replay would only sample model variability and is not authorized as a repair.
- Free Growth Writer authority now distinguishes approved creative realization from Canon mutation. Writer may create scene-level connective action, dialogue, sensory detail, pacing and minor non-Canon incidents needed to realize a pinned outline; it still may not contradict pinned facts, change an approved consequential outcome, introduce a new consequential Canon fact/identity/rule, or substitute evidence. Frozen deterministic acceptance before Live: 138 files / 906 tests passed, zero skips; typecheck, Prompt gates, production build and diff check passed.
- Live evidence `growth-guidance-live-2026-07-17T17-20-52-830Z.json` (SHA-256 `673785020B3E1CB898FD12D7FF848A9EBC3F1403CCDB3705C2EBBEF05B21C09B`; UTF-8 parse and `leakScan=passed`) proved the creative-authority change: C7-C9 Longform sections committed and the earlier repeated `missing_source` refusal did not recur. Longform then became blocked after C11 with exact reason `GROWTH_LONGFORM_PERSONAL_STORY_BINDING_SOURCE_INVALID`; C12 independently ended before retrieval as `PROVIDER_RUNTIME_FAILED`.
- Root cause boundary: generic Closure Revision could expose and mutate the Longform-managed `writing_constraints` document on a personal-story volume. Advancing that stable document version without atomically rewriting its binding assertion invalidated the source chain. Revision authority now keeps that document retrievable but omits it from editable targets, while Main rejects a forged mutation with `GROWTH_REVISION_POLICY_LONGFORM_DOCUMENT_FORBIDDEN`. Longform's own phase remains the sole writer of its managed outline. This repair has deterministic evidence only and requires a new frozen Live candidate.
- Frozen acceptance for that outline-only protection candidate: full `npm test` 138 files / 907 tests passed (876 unit, 22 integration, 9 E2E-support), zero failures and zero skips; typecheck, active Prompt publication (3), production build and diff check passed; Electron residue zero.
- Its one real replay produced `growth-guidance-live-2026-07-17T18-00-18-133Z.json` (SHA-256 `3482F1BF6F6FDCFE23D98C7DC73F5A50BFC189078B0BA32BC94D3DD05AF68A95`; UTF-8 JSON parse and `leakScan=passed`). C1-C4/C6-C9/C11 committed, C5/C10/C12 evaluated, nine Change Sets and nine checkpoint advances persisted, and the world map Job/Asset succeeded. The prior outline binding/source failure did not recur. After C11, Longform instead failed closed as `GROWTH_LONGFORM_SECTION_LENGTH_INVALID`; C12 recorded `GROWTH_CLOSURE_NO_PROGRESS` and the coordinator remained incomplete.
- New root cause boundary: generic Closure Revision could still edit `prose` documents owned by the personal-story `volume`, changing a managed section outside its pinned outline length range. Revision authority now keeps both the outline and volume-owned section prose retrievable but omits them from generic editable targets. Main applies the same `GROWTH_REVISION_POLICY_LONGFORM_DOCUMENT_FORBIDDEN` defense to a forged volume-prose mutation. Main-story prose remains editable because its owner is a `story`, not a Longform `volume`. This extension has 59/59 focused tests and typecheck evidence; it still requires a new full freeze and one new Live before acceptance.
- Frozen acceptance for the full managed-document protection candidate: full `npm test` 138 files / 908 tests passed (877 unit, 22 integration, 9 E2E-support), zero failures and zero skips; typecheck, active Prompt publication (3), production build and diff check passed; Electron residue zero.
- Its one real replay produced `growth-guidance-live-2026-07-17T18-27-58-053Z.json` (SHA-256 `A3C634902589A924BAABDA84883BFC274CBDF066600FBED04C915639B3FC1EDE`; UTF-8 JSON parse and `leakScan=passed`). It stopped before Closure continuation: C7 and C8 committed two Longform sections totaling 5,290 code points, while C9 exhausted three side-effect-free Writer corrections in the sequence `TOO_LONG → TOO_SHORT → TOO_SHORT`. Longform remained structurally `ready`; the managed-document invalidation did not recur. The map succeeded and no partial C9 Change Set was created.
- Product decision applied after that boundary: 7–8 千字的 aggregate story is sufficient and section-length fluctuation is acceptable. Each outline section's `estimatedCodePoints.max` is now advisory instead of a rejection boundary. The section minimum, 20,000-code-point hard safety ceiling, aggregate 7,000 minimum, no-padding/no-filler rules, evidence, order and replay checks remain authoritative. Deterministic evidence is 97/97 focused tests plus typecheck; a new full freeze and one real replay are still required.

- This batch does not add diagnostics to the separate Player Runtime projection; Player audit continues to reject this Growth/Agent-only internal audit operation to preserve Player Lens isolation.
- No installer/package or image-Provider success replay was run. The real text Provider replay reached the image tool boundary, but the image Job failed before a ready Asset existed.
- Before staging, split the Safe Diagnostic semantic batch from the pre-existing interactive-Growth WIP by reviewed file/hunk ownership. Do not use destructive Git cleanup and do not stage the protected evidence files.

### 2026-07-17 continuous diagnosis loop and hard stop

- A new standard loop is recorded in `docs/plans/2026-07-17-continuous-growth-diagnosis-repair-standard.md`: one exact fingerprint at a time, deterministic red/green repair, full freeze, then one real Live per candidate.
- Planned-Cycle start now retains one of ten content-free pre-Run diagnostic codes for state, checkpoint, rule, intent, Closure, Longform, Revision, Inquiry, anchor or binding failure. A deterministic checkpoint mismatch persists exactly one diagnostic, starts no Worker and keeps the Cycle planned.
- The first real candidate exposed 21 repeated `STEWARD_GROWTH_INQUIRY_REQUIRED` failures before the Cycle watchdog. Inquiry correction is now bounded to three side-effect-free attempts; the third diagnostic is terminal and transitions to one blocked final result.
- Inquiry Brief rejection now classifies only `INPUT`, `COUNT`, `ITEM`, `FRONTIER` or `TRANSITION` without retaining model-authored content.
- A subsequent real run reached Main Revision authorization and exposed `GROWTH_REVISION_POLICY_ASSERTION_SOURCE_INVALID`. Red/green characterization proved the Worker compiler emitted the local alias `@document1` instead of the pinned document evidence ID. The compiler now resolves existing document aliases to their authority evidence ID; same-proposal documents retain the controlled output placeholder. Main source policy was not relaxed.
- Latest frozen deterministic acceptance: 136 test files / 877 tests passed with zero skips; typecheck, active Prompt gates, production build and diff checks passed.
- Retained 2026-07-17 real evidence, all UTF-8 JSON parseable with `leakScan=passed`:
  - `02-54-33-090Z`, SHA-256 `50C332B3410B26766BCDC83FCB1EF15C65F3CA46B590581F3CE87290DC2E16EC`: unbounded Inquiry watchdog boundary.
  - `03-06-20-560Z`, SHA-256 `2D25E158FD27B62DDBA705E8F7D935F09FA9BEDEB32EEFBD7A11DDAB468A6944`: correction bounded to three attempts; C1 committed, C2 blocked.
  - `03-16-52-289Z`, SHA-256 `4D4F0D9EC1609359CD81E37D6BC313C4AD1F6F58848D37C22A654313DE6460D6`: Inquiry selected; exact Revision assertion-source policy rejection.
  - `03-22-19-617Z`, SHA-256 `76896405655DFEF50B836E54BE499F4781EF9A34545C76CAA3751D819EA589FF`: one `FRONTIER_INVALID` correction followed by a durable creator-choice block; Agent terminal projection timed out.
- Hard stop: creator choice currently terminalizes the Growth Cycle inside the tool path, while the Worker Run still relies on the model to call `submit_steward_result`. The real model did not guarantee that final call. Do not weaken the terminal-projection assertion. Recovery requires a product/Runtime decision on whether Main may actively stop that Worker and publish an authoritative blocked Agent terminal immediately after the creator-choice event persists.
- No commit, push, package or installer validation was performed. The mixed worktree remains unstaged.

### 2026-07-18 latest world-package export and Closure diagnostic candidate

- The next real interactive Growth run exports its committed Domain projection before the isolated workspace is removed. The fixed destination is `artifacts/latest-growth-world-package`; every run replaces the entire previous export atomically.
- The export contains stable committed documents, `graph.json`, a manifest and verified managed image bytes. A failed image is represented by the built-in placeholder plus `actualContent=false`; the placeholder is not treated as generated content. Provider stores, audit logs, prompts, databases and credentials are never copied.
- Closure evidence now retains only structural diagnostics: facet identifier, state, coverage and evidence-link count, plus Checker severity/category/link count. It does not retain summaries, repair prose, receipt identifiers, document text or model reasoning.
- The previous real candidate remains `growth-guidance-live-2026-07-17T18-58-06-850Z.json`, SHA-256 `9D40876D7F758447AEF1DE0B54A5C905A268E872A34C03F0899BCDFE5E96EADC`. It produced a structurally ready 8,751-code-point Longform and a successful world map, but three Closure evaluations all returned `continue_growing` and ended at `GROWTH_CLOSURE_NO_PROGRESS`. That evidence lacks the new per-facet diagnostics, so the exact unresolved Closure dimension remains unverified until one new run.
- Deterministic export and safe-projection tests pass 2/2; typecheck, active Prompt publication and diff checks pass. Full freeze and one real dual-Provider run remain required for this candidate.

### 2026-07-18 exported world package and Closure source repair candidate

- Real dual-Provider evidence `growth-guidance-live-2026-07-17T19-37-29-443Z.json` used `openai-compatible / gpt-5.4` and `openai-compatible-image / gpt-image-2`; SHA-256 `F2D547A9A066E745CD8B102A1F5B3C86A2E0F482B1ACE7F0D284177EA2223`; UTF-8 parsing and `leakScan=passed`.
- C1-C4, C6-C9 and C11 committed; C5/C10/C12 evaluated. Nine Change Sets and nine checkpoint advances persisted. Longform completed at 9,496 code points across three sections. One world-map Job and one 1536x1024 PNG Asset succeeded.
- The committed projection was atomically exported to `artifacts/latest-growth-world-package`: 10 resources, 10 stable documents, 6 assertions, 13 relations and one verified real map. The export is deliberately marked `incomplete` because Closure did not accept.
- The new structural diagnostic proved every resource/document/relation Closure facet was satisfied, while every fact facet remained `missing` with zero evidence in all three evaluations. The exported graph proved six formal assertions existed, including the personal-story binding.
- Root cause: Change Set application records stable-document evidence as `evidence_version`, while `GrowthClosureEvaluator` accepted only direct-repository `document_version`. The evaluator now accepts either kind only when the reference resolves to a stable document version visible at the pinned checkpoint. Unknown evidence IDs remain excluded.
- Automatic post-evaluation Revision now derives exact missing fact predicates and their world/main-story/focus-OC scopes from persisted Closure state. Worker compilation requires one sourced assertion addition per predicate, replaces model subject text with the trusted resource identity, and Main independently rechecks exact predicate/scope/subject/source presence. User-guidance revisions and unrelated Longform expansion do not receive this authority.
- Deterministic acceptance so far: 192/192 focused tests, typecheck, active Prompt publication and diff checks pass. Full freeze and one new real dual-Provider replay remain required before this repair can be called accepted.
