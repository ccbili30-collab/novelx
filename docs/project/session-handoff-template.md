# NovelX 新会话交接模板

用途：把任务交给另一个会话时，只提供必要上下文，避免重复加载全部聊天和全仓。复制本模板，填完后发送给执行会话。

## 必读入口

1. `CONTEXT.md`
2. 与任务对应的产品/架构文档：`<路径>`
3. 当前状态证据：`<notes/status 路径>`

不要先扫描全仓，不要把设计文档当完成证据。

## 唯一目标

`<一个可验收结果>`

## 当前事实

- 分支：`<branch>`
- 基线提交：`<hash>`
- 相关代码：`<路径>`
- 已验证结果：`<命令与数量>`
- 已知失败/阻塞：`<事实>`

## 允许修改

- `<文件或目录>`

## 禁止修改

- `<文件或目录>`
- 不进入 `<后续阶段>`
- 不改变 `<协议/Schema/权限/产品语义>`

## 实现不变量

- `<必须保持的行为>`
- 没有真实 Provider 时 fail closed。
- Fixture / Mock 不得标记为 Live。

## 验收命令

```powershell
<定向测试>
<类型检查/构建>
```

全量测试由主审查会话在合并冻结后集中运行，不得重复执行。

## 完成条件

- `<代码结果>`
- `<失败路径>`
- `<测试证据>`
- 返回修改清单、风险和未完成边界；不要自行提交、推送或进入下一任务，除非任务明确授权。

## 停止条件

发现产品歧义、公开协议/Schema/权限变化、数据迁移、文件所有权冲突、假 Live 风险或范围外根因时，立即停止并报告。
