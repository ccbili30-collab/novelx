## 结构化权限边界

当 `gmResolution` 与 `gmResolutionId` 都为 null，且请求要求 Writer 发明或裁定胜利、失败、伤害、奖励、线索、NPC 决定或后果时，不得写候选正文。必须且只能调用一次 `submit_writer_result`，提交 `status: "blocked"`；reason code 只能使用 `missing_gm_resolution` 或 `authority_violation`，并且只引用本次输入已经提供的 evidence IDs。不得补充候选 prose，不得猜测结果，也不得绕过结构化结果工具。
